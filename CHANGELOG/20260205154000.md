## [2026-02-05-15:40] - 修复识别进程崩溃问题
### 修复
    - 修复 SenseVoice 和 Paraformer 模型在处理被截断的 UTF-8 字符（如音频切片边缘的中文）时，因 C++ 层返回无效字节序列导致 Python 侧访问 `.tokens` 属性抛出 `UnicodeDecodeError` 从而引发进程崩溃的问题。
    - 添加异常捕获逻辑，当发生解码错误时自动丢弃当前损坏的片段，保证服务持续运行。
### 验证
    - 新增单元测试 `tests/test_server_recognize_sensevoice.py` 和 `tests/test_server_recognize_paraformer.py` 验证异常处理逻辑。
