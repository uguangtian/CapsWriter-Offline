## [2026-02-04-17:10] - 模型路径可配置化与迁移
### 新增
- 配置项 `model_paths.model_dir` 改为 `~/Documents/CapsWriter/modles`，并同步更新所有模型与 tokens 路径。
- `util/migrate_data.py` 支持迁移 `models` 目录到新路径。
- 新增 `tests/test_model_paths.py`，验证路径展开与参数读取使用已展开绝对路径。
### 修复
- `util/config.py` 对模型路径统一应用 `expanduser()`，避免 `~` 未展开导致的存在性校验失败。
### 优化
- `SenseVoiceArgs`、`ParaformerArgs` 使用 `ModelPaths` 的已展开路径字符串，避免配置插值问题。
