## [2026-02-01-21:51] - 修复语音识别中断问题及添加调试日志
### 修复
- 修复 `client_send_audio.py` 中 `finish` 任务在无缓存数据时 `final_data` 为 bytes 类型导致 JSON 序列化失败的问题。
- 修复 `client_send_audio.py` 中 `finish` 任务可能发送空数据导致识别中断的问题。
- 修复 `server_recognize_sensevoice.py` 中可能出现的 UnicodeDecodeError 导致进程崩溃的问题。
### 优化
- 在重启音频流导致中间数据未发送的情况下，客户端在 finish 阶段使用会话缓存补发最终音频，避免 data_size=0 的情况
- 在 `client_send_audio.py` 中添加详细日志，记录录音开始、积攒缓存、发送中间数据及 finish 数据的长度，便于排查问题。
- 创建 `tests/test_audio_processing.py` 单元测试，验证音频数据处理逻辑（降采样、立体声转单声道、Base64编码）的正确性。
