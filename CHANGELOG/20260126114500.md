## [2026-01-26-11:45] - Mac打包应用启动逻辑重构

### 修复
- 修复了 Mac 打包应用在启动时因缺少独立脚本文件而报错 `缺少必要文件` 的问题。
- 重构了 `start_unified.py`，增加了 `--internal-run-server` 和 `--internal-run-client` 参数，支持在打包后的单文件环境中直接调用内部模块启动服务和客户端。
- 修复了依赖检查逻辑，在打包环境下自动跳过脚本文件存在性检查。

### 优化
- 现在的打包应用是一个完全独立的二进制文件，不再依赖外部 Python 脚本。
