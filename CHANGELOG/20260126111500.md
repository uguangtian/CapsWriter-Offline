## [2026-01-26-11:15] - Mac打包修复与终端保持

### 修复
- 修复了 Mac 打包应用缺少 `mlx` 相关依赖导致可能闪退的问题。
- 修复了 Mac 打包应用在发生错误或退出时终端窗口立即关闭的问题，现在会等待用户按键确认。
- 优化了 `start_unified.py` 的错误处理逻辑，在打包环境下会打印完整堆栈信息并暂停。

### 优化
- 更新了构建脚本 `build_mac_app.py`，增加了 `mlx`, `mlx.core`, `mlx.nn`, `mlx_lm` 等隐藏导入。
