## [2026-02-04-10:00] - 路径可配置化及数据迁移
### 新增
- 配置文件 `config.toml` 新增 `transcription_result_path` 和 `audio_storage_path` 选项，支持自定义转录结果和录音文件的保存路径。
- 新增 `util/migrate_data.py` 脚本，用于将现有数据迁移到新配置的路径。
- 新增 `tests/test_path_config.py` 单元测试，验证文件保存逻辑是否正确读取配置路径。
### 优化
- 优化 `util/client_write_md.py` 和 `util/client_create_file.py`，不再硬编码路径，而是从配置中读取。
- 执行数据迁移，将原有 `CapsWriter-Offline` 根目录下的历史数据移动到 `~/Documents/CapsWriter/results` 和 `~/Documents/CapsWriter/audio`。
