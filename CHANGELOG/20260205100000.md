## [2026-02-05-10:00] - 修复App闪退及简化配置逻辑

### 修复
- 修复 `server_recognize_paraformer.py` 和 `server_init_recognizer.py` 中 `sys.stdout.flush()` 在无控制台环境下（GUI模式）可能导致应用崩溃的问题。
- 修复 `build_mac_app.py` 打包脚本，解决 PyInstaller 依赖安装问题，确保应用能正确生成。

### 优化
- 简化 `util/server_init_recognizer.py` 中的配置加载逻辑，移除从 `model_paths` 回退时的调试提示，避免干扰用户。
- 确保 `config_example.toml` 包含完整的 `[punc_args]` 配置，减少配置缺失带来的困扰。
