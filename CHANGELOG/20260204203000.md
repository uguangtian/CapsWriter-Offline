## [2026-02-04-20:30] - 修复标点模型加载失败问题

### 修复
- 修复 `util/server_init_recognizer.py` 中标点模型加载逻辑，增加对 `punc_args` 缺失的容错处理，现在会自动从 `model_paths` 中获取模型路径。
- 修复 `config_example.toml` 缺失 `[punc_args]` 配置块的问题，添加了默认的标点模型参数配置。
